/**
 * Created by youmc on 2017/1/16.
 */

export const customerGetter = {
  /*customer(state) {
    for(let i = 0, len = state.data.length; i < len; ++i) {
      let record = state.data[i]
      if(record.id == state.customerID) {
        return record
      }
    }
    return null
  }*/
}
